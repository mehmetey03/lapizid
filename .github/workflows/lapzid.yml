name: Lapizid M3U Oluştur

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Repoyu İndir
        uses: actions/checkout@v4

      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Kütüphaneleri Yükle
        run: pip install cloudscraper requests beautifulsoup4

      - name: M3U Dosyasını Oluştur
        run: python lapizid.py
        timeout-minutes: 90   # 🔴 MUTLAKA ARTIR

      - name: M3U Dosyasını Kontrol Et
        run: |
          echo "📊 Oluşan M3U dosyası:"
          if [ -f "dizipal_filmler.m3u" ]; then
            wc -l dizipal_filmler.m3u
            head -5 dizipal_filmler.m3u
          else
            echo "❌ M3U dosyası oluşturulamadı!"
            exit 1
          fi

      - name: Değişiklikleri Kaydet
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add dizipal_filmler.m3u
          git commit -m "✅ dizipal_filmler.m3u güncellendi - $(date '+%Y-%m-%d %H:%M')" || echo "Değişiklik yok"
          git push
